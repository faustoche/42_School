3. Implémentation de la logique d'exécution

- Méthode execute() dans AForm (approche élégante)
 Ajouter une méthode execute(Bureaucrat const & executor) const dans AForm
 Vérifier que le formulaire est signé (isSigned())
 Vérifier que le grade de l'exécuteur est suffisant (<= getGradeRequiredToExecute())
 Si les vérifications échouent, lancer les exceptions appropriées
 Si tout est OK, appeler une méthode virtuelle pure executeAction() ou performAction()

- Méthodes d'action dans les classes concrètes
 Implémenter executeAction() ou performAction() dans chaque classe concrète
 Cette méthode contient la logique spécifique à chaque type de formulaire
 Pas de vérifications dans ces méthodes (déjà faites dans la classe de base)

4. Modification de la classe Bureaucrat

- Nouvelle méthode executeForm()
 Ajouter void executeForm(AForm const & form) const
 Utiliser un try-catch pour capturer les exceptions
 En cas de succès : afficher "<bureaucrat> executed <form>"
 En cas d'échec : afficher un message d'erreur explicite avec la raison
